# Internal governance + design notes — machine-local, never shipped to the public repo
CLAUDE.md
MEMORY*.md
AGENTS.md
.claude/
.agents/
COALLEDGER_BLUEPRINT.md

# Generated SkillSpector scan reports (provenance recorded in SECURITY.md; the raw report is not shipped)
skillspector-*


# CC platform-generated skill lockfile (machine-local, never ship)
skills-lock.json

# claude.ai ZIP-packaging staging dir (scripts/build-claude-ai-zips.mjs, board #40) —
# a build artifact, zipped and attached to the GitHub Release, never committed
dist-claude-ai/

# Per-round return/working notes banked inside the room (the owner's r32 order names
# CoalLedger/scratchpad/ as the return path) — machine-local, never shipped.
# Flock-canonical: CoalMine's own .gitignore carries the identical `scratchpad/` line.
scratchpad/
